# API Configuration
VITE_API_URL=http://localhost:8000/api

# Feature Flags (optional)
VITE_ENABLE_DEVTOOLS=true

# add tools. do we have to prefacve the mwiith 'pizzaplace' like naming?
# agent_3501kgq8y9nre61vhwb60nyc0fnt      -> id of my agent. ideally run small script that uses hardcoded api key and all that tooo make updates to it