# LiveKit Cloud deploy descriptor.
# Generated by `lk agent create` — replace `subdomain` and `id` with values
# from your LiveKit Cloud project.

[project]
  subdomain = "REPLACE_ME"

[agent]
  id = "REPLACE_ME"
